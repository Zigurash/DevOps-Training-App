{
  "name": "realworld-infra",
  "version": "1.0.0",
  "private": true,
  "description": "Realworld Infrastructure Lab - DevOps/SRE laboratory application",
  "scripts": {
    "dev": "npm run dev --prefix backend & npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "test": "npm run test --prefix backend && npm run test --prefix frontend",
    "test:e2e": "npm run test:e2e --prefix backend",
    "lint": "npm run lint --prefix backend",
    "format": "npm run format --prefix backend",
    "db:migrate": "npm run db:migrate --prefix backend",
    "db:migrate:dev": "npm run db:migrate:dev --prefix backend",
    "db:seed": "npm run db:seed --prefix backend",
    "db:generate": "npm run db:generate --prefix backend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "docker:up": "docker compose up --build -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f"
  }
}
